import AdminLoginContent from "@/components/admin-login-content";

export const metadata = { title: "Admin login — DIY vs PRO" };

export default function AdminPage() {
  return <AdminLoginContent />;
}
